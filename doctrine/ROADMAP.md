# Roadmap

## The goal

Flightdeck takes any Jira board and any workflow it is pointed at, watches it and its error sources continuously, and turns every ticket into a merged PR handed to QA with the least wall clock and the fewest tokens, with no intervention from Aaron except to unblock something and to fix flightdeck itself. The conductor is a master of the board: it sees every new ticket, comment, alarm and error within one poll interval and acts on it. A blocker is the top priority the moment it exists. The current BBZ backlog is the first workload, not the design target.

Aaron, 2026-09-08. Only Aaron edits this paragraph.

<!-- goal-sha256: 87fdcb9e0778c4432fdd75c3709d69394a97ed4936ebee29f1ad7f8cfd8c8bf8 -->

## Items

| id | delivers | serves | status | pr | proof |
| --- | --- | --- | --- | --- | --- |
| R-01 | doctrine/ROADMAP.md exists with these sections | autonomy | running | this PR | file on main |
| R-02 | roadmap guards: briefs for this repo need a `roadmap: R-nn` line, `npm run check:roadmap` in verify, conductor parks off-roadmap lanes, self-loop findings without an id land under Proposed | autonomy | review | #88, #91 | guard #1 (brief line), guard #2 (`check:roadmap` in verify), guard #3 (off-roadmap lane park), guard #4 (Proposed routing) all shown red on a specimen then green |
| R-03 | design canvas of every console surface and dialogue | autonomy | done | https://claude.ai/code/artifact/1dad754e-dd7b-4c09-aa26-c09ba51a7737 | canvas saved 2026-09-08; Aaron's Claude Design revision imported to `doctrine/design/` |
| R-04 | the three UX rules as tests (identifier lint, one shared question card, one action per card state) | autonomy | running | | red on today's board, then green per fix |
| R-05 | auto-merge honours FORGE_COUNCIL_AUTOMERGE | autonomy | done | #77 | Q-56440c7b merged with mergedBy queue, PR #79 |
| R-06 | the packet carries comments, assignee and links; In Review/QA scopes from the last QA comment | autonomy | planned | | a specimen ticket in QA planned from its last comment |
| R-07 | `handoff` item kind: comment, assign, transition, done, no worktree | autonomy | planned | | a handoff item lands done with no worktree |
| R-08 | the handoff comment carries the worker's real visual plan; JoeHandoff posted on backend tickets; OTA outcome written back | autonomy | planned | | Haiping's ticket shows numbered steps |
| R-09 | decisions on evidence: `decide:` line, `## Decision` posted before launch | autonomy | planned | | a Lane A ticket whose first comment is the decision |
| R-10 | width 8 and the fixed two-by-four running grid with idle cards | clock | planned | | eight cards, idle slots say why |
| R-11 | intake bridge to the queue store; Jira watcher at 30 s; a comment on an owned ticket becomes a send; Done closes the lane | clock | review | #97, #100 | a comment reaches its lane within one poll |
| R-12 | Sentry and CloudWatch feeds with watermarks and lane cards | any-board | planned | | a hand-flipped alarm is one event |
| R-13 | `event` kind: triage session, verdicts, `create` on the Jira client, form-shaped filing | autonomy | planned | | a synthetic Sentry issue becomes a ticket with a finding |
| R-14 | digest lane: noise ledger folded once a day into one Jira comment | tokens | planned | | the digest posts once with its card's count |
| R-15 | supply rule: blockers first, then the back-fill order | clock | planned | | a free slot fills in the stated order |
| R-16 | console surfaces rebuilt to `doctrine/design/Flightdeck Console.dc.html` | autonomy | done | #101 | 30 screenshot pairs in the PR, merged 2026-09-09; layout edge cases from the council fixed in the same PR |
| R-17 | conductor tools: repo read, tests, forge CLI, every console route, self-brief authoring | autonomy | planned | | the conductor clears a blocker with no person |
| R-18 | project files and workflow discovery; every reader off FORGE_* | any-board | planned | | a second Jira project runs one ticket with no code change |
| R-19 | a worker's leftover uncommitted files never park the item at the gate | autonomy | running | | the two 2026-09-08 cases pass |
| R-20 | a goal-source item continues to its successor on a context handoff | autonomy | planned | | Q-17bb4283's shape passes |
| R-21 | the console cuts over to a new head on a quiet interval, not only when the queue is idle | clock | planned | | no hand restart after a merge |
| R-22 | the Merge click lands through git (fetch, squash, commit, push) instead of `gh pr merge`, so a GitHub mutation rate limit never blocks a reviewed PR; gate/council/PR-discovery still call `gh` (not yet moved off the API) | autonomy | review | this PR | real bare-remote fixture: item reaches done, mergedBy queue, base ref advanced, gate never called |
| R-23 | `forge clock` report: critical path per item, worker vs runner suite runs, tick stall | clock | planned | | a run of `forge clock` prints per-item hop medians and the critical path |
| R-24 | concurrent item advance; Codex lane runs beside the lenses | clock | planned | | a 21-minute council round no longer holds every other item's launch, status read and gate |
| R-25 | the runner's verification is the one full run; worker limited to targeted tests by rule; suite output to file | clock, tokens | planned | | full-suite runs per item drop to one runner run plus CI |
| R-26 | warden stale-session joins run state (open tool call, pid) into the process loop; tool-budget kept | autonomy | planned | | a worker inside a long sanctioned Bash call is never parked as stale |
| R-27 | pending checks are a wait state; rebase only when behind | clock | planned | | review→end median moves toward CI duration, no park on a still-running check |
| R-28 | queue passes the risk decision to council (no forced Codex); changed-lines count excludes lockfiles; small-PR path proven | tokens | planned | | a 20-line non-risky RN PR plus a lockfile change sizes as small |
| R-29 | self loop records a refusal once and re-checks on head change | tokens | planned | | `self.merge-refused` stops re-polling every 5 minutes on an unchanged PR |
| R-30 | ticket planning retries a transient fetch; reason on the fleet row | clock | planned | | a `fetch failed` plan hop retries with backoff instead of failing the item |
| R-31 | warm-template provisioning by copy | clock | planned | | a matching `package-lock.json` hash skips `npm ci` for a copy |
| R-32 | burn.mismatch once per change; cached journal readers | clock | review | this PR (readers) | `replay()` is incremental per path: a 20 MB journal that took 16.8 s per call on the 4120 server's thread reads only its appended bytes after the first call (`tests/forge/journal-replay-cache.test.ts`); the burn.mismatch half is still open, 873 rows in one hour on 2026-09-09 |
| R-33 | hook tax measured then batched into one fail-closed dispatcher | clock | planned | | per-Bash-call hook overhead drops from 2.1 s to under 400 ms, no guard weakened |
| R-34 | fix rounds wired with a CI failure classifier | autonomy | planned | | a FIX FIRST verdict launches a same-worktree repair round instead of parking forever |
| R-35 | one PR snapshot per item, gh reads 8 to 4 | tokens | planned | | a clean PR's advance uses 4 `gh` calls instead of 8 |
| R-36 | council runs while CI runs, attest only on exact head+base+green CI | clock | planned | | PR→merge time drops to max(CI, council) with no stale-head attestation |
| R-37 | per-item event-driven actors on top of R-24's leases | clock | planned | | an item advances on its own wake event instead of waiting for the next tick |
| R-38 | completion schema and deterministic finalizer, routines matched by repo kind | tokens | planned | | `forge_done` evidence validates against a schema; an RN brief carries no roadmap routine |
| R-39 | auto-compaction probe with the fleet threshold below the class ceiling | tokens | planned | | a probed session compacts before hitting the worker's context ceiling |
| R-40 | no merge on a stale base: the gate re-runs the checks on the merge result when the base advanced since the PR's last green run | autonomy | planned | | a PR whose base moved is re-verified before merge, and `main` is never left red |
| R-41 | `check:roadmap` fails a run only for the branch under test, never for another open PR's body; `verify`'s steps and CI's steps are the same list | autonomy | planned | | a worker's `npm run verify` cannot fail because another draft PR cites no R-id |
| R-42 | a self-fix may never delete or skip a test to clear a red check; it fixes the code or parks and asks | autonomy | planned | | a specimen self-brief that deletes a failing test is refused at the gate |
| R-43 | AI accounts section in Settings, a rate limit remembered on disk across processes, and every session picking an account that is not limited | autonomy | done | #116 | 13 cases in `tests/forge/accounts-failover.test.ts`, proven red first; the 1e pair shows a ready row and a limited row |
| R-44 | account limits read live: every window the provider reports (session, weekly, weekly per model) with its percent and reset time, on the Settings row and in account selection | autonomy | done | #119 | `accounts-probe.ts` reads Claude's `oauth/usage` limits array and ChatGPT's `wham/usage`; specimen bodies in `tests/forge/accounts-probe.test.ts`; the 1e pair shows three rows with bars and reset times |
| R-45 | the artboard gaps left after R-16: decision card event line, the agent's own voice in the board rail, per-call tools list, confirm card body and meta, blocker card actions that act, lane-sheet question context | autonomy | planned | | each artboard's element renders from real state, with its pair reshot |
| R-46 | every console surface serves three registers -- a glance sentence, a detail sentence behind a disclosure, and the raw fact record under `?verbose=1` -- written by the model through `reasonerFor` class `narrate`, refereed against the facts before it is served, cached by the facts and never by the clock, with no route ever awaiting a call | autonomy | done | #106 | checker specimens (dropped PR number, moved clock time, invented count, swapped state word, leaked run id) each named their token then the design's own copy accepted; store falsifiers (never-resolving queryFn, one call for two reads, zero on a second poll, the 301st key capped, restart makes zero calls for known keys); live proof on 2026-09-09 against the real model: 20 `reasoner.call` rows on the first pass, 0 on the second, 19/20 accepted |
| R-47 | the width stepper answers the click at once and posts once when the clicking stops | autonomy | done | #125 | six to ten is one post of 10, and the number moves before the round trip |
| R-48 | every Jira comment and PR write flightdeck makes passes the Joe readability rule, refusing on DENY, contract and specimens loaded from the machine rather than shipped in the repo | autonomy | done | #129 | refusal tests watched red then green for the Jira write client, the PR-comment writer and the worker's own `gh pr create`/`comment`; a neutral in-repo specimen set for the repo's own tests, the real contract installed to `~/.forge/readability` by dev-harness |
| R-53 | the console as a Windows service with graceful stop (SIGINT/SIGTERM/SIGBREAK clear the tick, close the server, release the queue lock, journal `console.stopped`); a logon-time login helper for the browser step a service in Session 0 cannot do | autonomy | done | #135 | `tests/forge/service/shutdown.test.ts`, `tests/forge/service/install-script.test.ts`, `tests/forge/login-helper.test.ts`, `tests/forge/server-login-helper.test.ts` red then green; the NSSM zip's sha256 verified by direct download and hash |
| R-54 | the drift judge reads the run's literal transcript tail, not just tool names, and acts: `drifting` nudges, a confirmed `off-brief` parks | autonomy | done | #135 | `tests/forge/transcript-drift.test.ts` red then green, including the off-brief specimen invisible in tool names whose unique path is asserted present in the judge's actual prompt |
| R-55 | per-run wall clock (`RunState.startedAt`, `maxWallMs` per class, parks once) and an orphan sweep that only ever touches a finished run's own process subtree | autonomy | done | #135 | `tests/forge/session-clock.test.ts`, `tests/forge/sweep.test.ts` red then green, including the deep-ancestor-chain specimen (a pid reused by a live session two levels up from the target) |
| R-56 | an async Codex advisor (`ask`/`status`/`result`) the Conductor agent can call as `ask_codex`, never blocking a tick | autonomy | done | #135 | `tests/forge/council/codexAdvisor.test.ts` red then green (`ask` returns before `result` would ever resolve); one live `ping`-class call, result `pong`, exit 0 |
| R-59 | flightdeck gates its own Jira and PR writes on the Joe rule again, contract and specimens loaded from the machine | autonomy | done | #138 | agnostic check exit 0; neutral specimen suite pinned at 8; installer test red then green; unconfigured journals `readability.unconfigured` and a status line is unit-tested (not yet a console row -- see `console/readability-status.ts`) |

## Not in scope

- Sentry and CloudWatch straight into the queue as their own items
- every event filing a ticket with no triage
- a new Intake view instead of lanes on the board
- a Conductor-rail-only event feed
- filing on the raw event with no investigation
- a decision sheet for Aaron instead of deciding on evidence
- the conductor editing the live console directly from chat

## Decisions

- 2026-09-08: product calls on Aaron's tickets are decided on ticket evidence, commented in his voice, built, reversible by reply.
- 2026-09-08: vendor-blocked tickets get the exact ask commented and are reassigned to the owner.
- 2026-09-08: Sentry and CloudWatch events are triaged and investigated, then filed as tickets in Harrison's form shape, noise to a daily digest.
- 2026-09-08: sources are lanes on the board.
- 2026-09-08: eight agents at all times as two columns of four, three watchers and five workers.
- 2026-09-08: every question in the UI is multiple choice with one recommended option.
- 2026-09-08: no identifier is ever a primary label.
- 2026-09-08: the conductor fixes flightdeck through the self loop, never by editing the live console.
- 2026-09-08: blockers outrank every ticket.
- 2026-09-08: per-board config in `~/.forge/projects/<KEY>.json` with workflow discovery.
- 2026-09-08: both merge allow-lists (`FORGE_COUNCIL_AUTOMERGE`, `FORGE_QUEUE_MERGE_REPOS`) must name a repo for it to auto-merge.
- 2026-09-08: the docs repository's roadmap on branch `docs/amp-g2-implementation` is retired by this file.
- 2026-09-09: Codex lane is risky and large diffs only, except when it's something that deals with money, which always needs the highest and most aggressive review (Aaron). Money paths always get the full council — 3 lenses, Codex lane, Opus judge, all fix rounds — regardless of size.
- 2026-09-09: R-23 to R-32 promoted from the wall-clock audit; first wave R-23 to R-26.
- 2026-09-09: pass-2 review (Claude + Codex): ticks overlap on stale snapshots, so tick
  coalescing and per-item leases (R-24) precede any concurrency; attestation reuse
  across a rebase is rejected; the Codex lane timeout stays at 900 s; the queue's git
  auto-merge is allowed only when bound to the attested head and base.
- 2026-09-09: a green PR is not a green merge. `main` went red when a PR whose checks predated a rebuilt component merged on a stale base, and again when a draft merged an import whose file was never committed. A red `main` blocks every queue merge. Requiring branches to be up to date is rejected because it serialises the fleet at width 6 to 12; the gate re-verifies only when the base advanced (R-40).
- 2026-09-09: the roadmap guard must not couple one PR's verification to another PR's body, and CI must run the same steps as `verify` (today it skips `typecheck:console`, `check:ux`, `check:roadmap` and the desktop tests, so a green CI is narrower than a green verify) (R-41).
- 2026-09-11 (Aaron): the graph replaces the board; status leaves the chat; questions one at a time in a top strip; planning is an adversarial interview; a question Aaron cannot answer is passed by a click to a teammate in the private `#fd-questions` Slack channel and the reply comes back on its own; the graph is React Flow; first wave R-75 and R-76 in parallel, R-74 second. Spec: `doctrine/design/operator-experience.md`.
- 2026-09-11 (Aaron): manual first. Every pipeline stage is a button with an auto toggle, off by default; the fully automated line is every toggle on (R-79). Aaron works the frontend only; backend questions and tickets go to Joe and Jason.
- 2026-09-11 (Aaron): fully optimistic UI with the standard fallback on every action, and the server side to match: accept first, work second, roll back with the reason on failure (R-80).
- 2026-09-09 (Aaron): a self-fix may never delete a test to go green. The self loop's answer to a red trunk was to delete the failing rail-hygiene test, which was reporting a real regression: the rebuilt rail had lost its Activity drawer. Fix the code, or park and ask (R-42).

## Proposed

Self-loop findings that cite no R-id land here; only Aaron promotes them.

## How to use this file

- Read this file before any flightdeck work.
- Every brief carries `roadmap: R-nn`.
- A change to the goal paragraph without Aaron's own words in the commit is a defect.
- Update an item's status and pr in the same PR that changes it.
| R-46 | accounts by provider: link a Claude or a ChatGPT login from Settings with no label, named by its email; Codex reviews run under the linked ChatGPT account with the most headroom; the machine's own login shown as a row | autonomy | done | #119 | `startConnect('codex')` spawns `codex login` under its own `CODEX_HOME`; `codexLane.ts` sets `CODEX_HOME` from `configDirForSession(..., 'codex')`; the fleet row carries `fleet: true` |
| R-49 | session registry across config dirs: `scanSessions` reads `~/.claude`, `~/.claude-fleet` and every `provider: claude` entry in the accounts registry, checks each pid alive, marks a dead one vanished | autonomy | done | #132, #140 | `src/forge/sessions/registry.ts`; `tests/forge/sessions/registry.test.ts` |
| R-50 | one ledger: `session.*`/`message.*` events fold into `fleet.jsonl` via `POST /sessions/event` and `POST /journal/append`, dual-written from dev-harness's `coordination/ledger.py` | autonomy | done | #132 | `src/forge/sessions/ingest.ts`, `journal.ts`'s `sessions` fold, `server.ts` routes |
| R-51 | exit classification and cleanup: `SessionEnd.reason` crossed with `closed_with_complete` classifies `done`/`abandoned`/`continuing`/`interrupted`/`unknown`/`killed`; a killed or interrupted session runs `coordlib.py sweep` and reports (never deletes) its worktree | autonomy | done | #132 | `src/forge/sessions/exit.ts`, `sessions/cleanup.ts`; `tests/forge/sessions/exit.test.ts`, `ingest.test.ts` |
| R-52 | message record and orchestrator-to-terminal delivery: `SendMessage` calls journal `message.sent`; `POST /sessions/:id/message` queues, `GET /sessions/:id/inbox` drains on the session's next prompt; `GET /sessions` marks two live sessions on one repo "may need to merge" | autonomy | done | #132 | `sessions/inbox.ts`; `tests/forge/sessions/routes.test.ts`, `console/sessions-view.test.ts` |
| R-57 | every terminal picks its login: a `claude` shim on the PATH asks the console (or the same rule offline) which login has room, prefers the operator's own while it is under 90%, prints one plain sentence naming it, and never re-routes `-p`, `auth`, `mcp`, `agents`, `daemon` or `--version`; a newly logged-in directory is seeded with junctions to the operator's transcript, hook, skill and plugin trees and copies of `settings.json` and `CLAUDE.md`, so a second login is not a stripped-down machine | autonomy | done | #134 | a fake `claude.exe` proves arguments pass verbatim and the exit code comes back; `-p` and `auth` reach it with `CLAUDE_CONFIG_DIR` untouched; the console answer wins when up and a real closed port falls back to the offline rule; the printed sentence carries a percent and no uuid, and the Python and TypeScript sentences match on two different percentages; seeding refuses a directory with a real `projects/` folder |
| R-58 | a login that runs dry does not stop the session: the console writes one marker per live terminal on that login naming where to come back, the shim reads it once after its child exits and relaunches under the new directory with `--resume`, and a shared `projects/` tree means the conversation survives the move | autonomy | done | #134 | resume across config directories proved live on CLI 2.1.267 before anything was built on it; two live sessions on a limited login yield two markers and a queued line, an unlimited login yields none, and no candidate yields a marker with a null target that says so; a marker older than ten minutes does not relaunch, and one left for another login does not either |
| R-59 | accounts finished in Settings: hold-back and a concurrency ceiling editable per row, unlink keeps the login's files and a separate confirmed delete removes them, and an exhausted fleet refuses the launch and names the reset instead of borrowing the machine login | autonomy | done | #136 | `updateAccount` writes both fields and the on-disk registry is asserted after the patch; a non-empty fully limited registry refuses and an EMPTY one still falls through to the machine login; the leftover delete refuses `..`, an absolute path and a nested segment, proven by removing the check and watching all ten go red |
| R-60 | Machine page: every session on the machine and its process subtree, live -- observation only (no kill/park/nudge), read every 10s, output stated "not captured" until goal 2 | autonomy | done | #137 | `tests/forge/sessions/started-row.test.ts`, `tests/forge/console/sessions-view.test.ts`, `tests/forge/machine/snapshot.test.ts`, `tests/forge/machine/route.test.ts`, `tests/console/machine-view.test.tsx` all red then green; `descendantsOf` imported not reimplemented; command lines masked before storage |
| R-61 | board freshness: a finished lane whose queue item has left the active queue log gets exactly one final PR re-check (sourced from the cache's own last-known repo/PR, never a live queue item), and a PR closed without merging is retireable, not stuck like a genuinely open one | autonomy | done | #141 | `tests/forge/console/reads.test.ts`, `tests/forge/console/retire.test.ts`, `tests/forge/console/pr.test.ts` red then green; `finalCheckAt` stamped whatever the re-check finds, proven terminal against a concurrent sibling cache write |
| R-63 | board freshness, part 2: a lane stuck `blocked` (worker died mid-tool, never reaches a finished state) with a known, stale-unmerged PR now keeps getting re-checked on the same ongoing cadence an active queue-item lane already gets, instead of never again; a legacy cache row with no `repo` field falls back to parsing it off the PR's own `url` | autonomy | review | this PR | `tests/forge/console/reads.test.ts`, `tests/forge/console/pr.test.ts` red then green; live-verified against BBZ-226/PR#107 (merged 2026-09-08, board read `merged: false` for 3 days) |
| R-64 | board freshness, part 3: `pr.closed` (added in R-61) wired into every remaining place that only ever checked `pr.merged` -- `mergeableFor`, `mergeReadyReportFrom`, `nextCategoryFor`, the orphan-lane sweep, `computeReadiness`, and every `plainStatus`/`plainForQueueItem` sentence that read "is open" for a PR that had already closed | autonomy | review | this PR | `tests/forge/console/lanes.test.ts`, `laneGlance.test.ts`, `tests/forge/rounds.test.ts`, `tests/forge/console/summary.test.ts`, `tests/forge/console/plain.test.ts` red then green |
| R-65 | console startup never strands the window: `GET /health` (built dist only) plus `probeHealth()`'s four states replace the old any-200-is-up probe; the load loop only loads once `up-healthy` and returns a non-2xx or failed main-frame load to the status window with one exported sentence, never raw JSON; `bringUpConsole` reads the queue lock before ever starting a second console; `forge up` exits 76 naming the port holder instead of exiting 0 on a swallowed bind rejection; the fallback path builds before starting and a launcher pre-check skips build+start when `/health` is already 200 | autonomy | review | this PR | `tests/forge/server-health.test.ts`, `tests/forge/cli-up-guard.test.ts`, `desktop/electron/__tests__/probe.test.ts`, `load-loop.test.ts`, `console-watchdog*.test.ts`, `console-supervisor*.test.ts`, `decide-console-action.test.ts`, `launcher-plan.test.ts`, `checkout-file.test.ts`, `locate-checkout.test.ts` all red then green; live e2e against a real spawned console on an ephemeral port (never 4120) proved a cold start, a not-built-yet console, and a killed-and-revived console all recover without a click; a foreign process on the port is detected and never silently attached to (the confirm-and-replace half of that case has no UI yet and is not offered) |
| R-62 | the session registry is the truth about which Claude sessions are alive: a session the scan stops returning is ended once its recorded pid is gone, and a session a real `SessionEnd` ended is never brought back and never reclassified `killed` | autonomy | review | this PR | `tests/forge/sessions/reconcile.test.ts` red then green on five new cases; on the live console `GET /sessions` fell from 37 live rows to 9 and its terminal set then matched `claude agents --json` exactly across three checks; a hard kill was journalled in 3.3 s and a Ctrl-C kept its own exit class |
| R-71 | the board shows watcher status in the header, a Full re-sync and start button, and a sync card with Re-sync on every page -- Aaron: "i should be able to see and monitor all of this as well ... it all needs to be operateable from the GUI" | autonomy | review | this PR | `tests/console/sync-text.test.ts`, `tests/console/watcher-status.test.tsx`, `tests/console/chrome-sync.test.tsx`, `tests/console/sync-card.test.tsx`, `tests/console/app-sync.test.tsx` all red then green; renders only against `stub-server.ts` fixtures typed off `src/shared/sync-contract.ts` -- no agent looked at the rendered page |
| R-66 | R-65 escape: `up-healthy` now always attaches, lock owner or not -- a launcher-managed console can hold the intake queue lock for its whole lifetime, not only while starting, and the app's own environment can disagree with that console's about whether the queue is on; clicking the real shortcut wedged on "waiting for it to become healthy" forever against a fully healthy, fully serving console | autonomy | done | this PR | `decide-console-action.test.ts` red on the unpatched code (asserted `wait`, reproducing the live bug), green after; live-reproduced by Aaron clicking the real shortcut against the real console before the fix |
| R-67 | the board stops repeating itself: ALL THREE dead-worker branches now read the same relaunch count and go to the judge at `maxRelaunches` instead of relaunching every round forever -- a `killed`/`exhausted` lane (BBZ-223 relaunched on the identical line for 12 h) and an item whose launch never registered a lane at all, which was the branch the first fix missed; and an unclicked Confirm survives the console restarting under it -- kill and retire proposals carry a serializable descriptor persisted to `pending-confirms.json` and are rebuilt by whichever process the operator clicks against, ending the `refused: nothing pending for <uuid>` answers | autonomy | review | this PR | `tests/forge/rounds.test.ts` (cap trips at `maxRelaunches` and still relaunches one below it, for a `killed` lane, an `exhausted` lane and a no-lane item alike) and `tests/forge/server-command.test.ts` (a token minted before a real `ForgeServer` close+restart runs after it; an unminted token still refuses; a token spent before the restart cannot be spent again; a rebuilt kill that the fleet refuses answers non-200; a rebuilt kill-and-remove still removes when the run ended on its own) -- each watched red on the unpatched code and each detector re-proven by neutering only the fix all red then green, each watched failing on the unpatched code; `npm run verify` green | 
| R-68 | resync-core (stream A of the re-sync program): the Jira watcher now scopes to `assignee = currentUser()` plus its own owned keys instead of the whole board, runs at runtime via `POST /watcher/on\|off` instead of boot-only, and never re-adds a done ticket; `GET/POST /sync` wipes the queue, resets watermarks, pulls Jira and starts the pipeline behind the existing confirm gate, journaling `sync.started/sync.stage/sync.finished`; `fetch-repos`/`reconcile-prs`/`sweep-worktrees` (stream B) are the one permitted placeholder, always `skipped`, never `ok`, until that stream merges | autonomy | review | this PR | `tests/forge/intake/watcherWire.test.ts`, `tests/forge/sync/{queue-wipe,watermark-reset,watcher-state,run,jira-pull,stop-workers,routes}.test.ts`, `tests/forge/server-events.test.ts` all red then green; `/critique` on `run.ts` found and fixed a real `running`-set leak (reproduced red, fixed, regression-tested green); one live read-only `GET /rest/api/3/project/BBZ/statuses` confirmed the "In Review/QA" status name; nothing here was exercised against the live console on 4120 |
| R-69 | re-sync, stream B: `src/forge/sync/code/{fetch,reconcile,worktrees,real-deps,index}.ts` -- pure functions a sync runner calls with injected `CodeSyncDeps`, never real git or gh from a test. `fetchRepos` fetches every configured repo's base branch, carrying one failure without stopping the others. `reconcilePrs` classifies each ticket key's PR per repo as shipped/open/none (a `hotfix-` id resolves through `branchFor`; a gh error for one key lands in `none` and logs a failed count, never losing the others). `sweepWorktrees` removes a worktree only when its branch's PR is merged or closed AND the tree is clean and pushed AND the path is unclaimed both at the initial `claimedPaths()` snapshot and again immediately before removal (`claimed-after-snapshot`); every other case (dirty, unpushed, claimed, pr-open, no-pr, main-checkout, gh-error) is kept; `dryRun: true` returns the same decisions with zero `worktree remove` calls; removal is `git worktree remove <path>` then `git branch -D <branch>`, never `--force`. `real-deps.ts` builds `git`/`gh` on `execRun` exactly as `self-wire.ts:114-126` does and reads `claimedPaths()` from the coordination sessions dir, 10-minute heartbeat freshness. A three-lens `/critique` on the removal-decision design found four real bugs, all fixed: an uncaught `worktreeStatus` throw for one worktree used to abort the whole sweep (now a per-entry `status-error` kept row); `gh pr list --head` can return more than one PR when a branch name is reused, and an OPEN row now always outranks a stale merged/closed one; claimed-path comparison was exact-string (a backslash, trailing-slash or case difference from a claim written by a different tool would silently miss a live claim and delete its worktree) and is now normalized the same way `coordlock.ts#normalizeCheckoutPath` already does; a genuinely detached-HEAD worktree was mislabeled `main-checkout` and now gets its own `detached` reason, with the actual main-checkout check now comparing against the repo's configured checkout path (not just "first porcelain entry"), closing the brief's "any path in the main-checkout list" gap. | autonomy | review | this PR | `tests/forge/sync/code/worktrees.test.ts` (one row per reason, table-driven, fake git rejects any unauthorized `worktree remove` or `--force`, `branch -D` asserted once per removed row and never for a kept row, dryRun asserted zero remove calls, plus the four critique-found cases above) all red then green, `reconcile.test.ts` (merged/open/closed/none, two repos for one key, `hotfix-` id, a gh error for one key never loses the others) red then green, `fetch.test.ts` (one of three fetches fails, the other two carried, the failure's message kept) red then green, `real-deps.test.ts` (exact argv asserted against a fake `execRun` for both `git` and `gh`; `claimedPaths` against a temp sessions dir returns only the fresh-heartbeat claim) red then green; `npm run typecheck` clean on the new files. `src/shared/sync-contract.ts` (stream A, PR #149, merged 2026-09-11) exists on this base now -- the type-parity test at `tests/forge/sync/code/contract-shape.test.ts` proves `CodeSyncDeps` matches it exactly. |
| R-70 | every board page gets its own re-sync: `syncSessions`/`syncAccounts`/`syncMachine`/`syncInbox`/`syncLanes` in `src/forge/sync/pages/`, each a pure function over injected collaborators, with `buildPageDeps` binding them to the real scan/fold/ingest/sweep, `AccountsService`, `buildMachineSnapshot`, `fetchInboxIssues`+`classifyInbox`, and `consoleReads.lanesResponse`/`runRecheckResponse`; a new `cli.ts` timer re-probes every account every `FORGE_ACCOUNTS_PROBE_S` (default 600) seconds and journals `accounts.probed`; nothing here calls the live console on 4120, and the inbox message states plainly that no Sentry client exists (R-13) | autonomy | review | this PR | `tests/forge/sync/pages/*.test.ts` (all six work items and `buildPageDeps` red then green; `syncLanes` proven sequential by a start/resolve interleaving array, `syncAccounts`'s cli.ts timer proven firing under fake time) and `npm run verify` green |
| R-72 | the console never falls back to a checkout nobody chose: a configured candidate that is not a checkout, or two that disagree, is a refusal naming what was found and where, instead of silently ranking one above the other; the install directory is used only when nothing is configured, and an unconfigured machine still reaches the pick-a-folder path; a start now prints the directory it is serving, and the app logs the checkout and which setting chose it | autonomy | review | this PR | `desktop/electron/__tests__/locate-checkout.test.ts` (broken candidate refuses rather than taking the next; disagreeing candidates refuse naming both; the refusal names the setting and the path; agreement resolves with its source; nothing configured is its own outcome) and `tests/forge/launcher.test.ts` (`runtimeCheckout` reports the root git names and falls back to its own directory rather than saying nothing) all watched red first, each detector re-proven by restoring the old ranking and watching the same cases go red again; `npm run verify` green | 
| R-73 | resync-wireup (stream E, last of the re-sync program): `buildProductionSyncStages` now binds `fetch-repos`/`reconcile-prs`/`sweep-worktrees` to stream B's real functions and all five page scopes to stream C's real `buildPageDeps` functions, so no `full`-scope or per-page stage ships `skipped`; `reconcile-prs` checks the ticket keys the queue held right before `wipe-queue` cleared it and its `shipped` result now feeds `pull-jira`'s `shippedKeys`; the `POST /sync/full` confirm blast now names a real `sweepWorktrees(deps, { dryRun: true })` count instead of the "not wired yet" placeholder stream A shipped | autonomy | review | this PR | `tests/forge/sync/wiring.test.ts` (all nine `full`-scope stages asserted individually not-skipped against a production wiring built with injected `execRun`/sessions-dir/registry/consoleReads fakes; the unconfirmed `POST /sync/full` blast asserted to carry the dry-run count from two fake eligible worktrees) watched red on the unmodified wiring (every stream B/C stage skipped) then green; `grep -rn "not wired yet" src/forge/sync` empty; `npm run verify` green |
| R-74 | Flow page: the Board, Queue and Blockers tabs fold into one React Flow row per ticket (Intake, Plan, Worktree, Worker, Verify, PR, CI and Council side by side, Merge, Deploy, Handoff, Done), each node in exactly one of idle, active, done, waiting, failed or needs-you, repair badges from the journal rows that already exist, a click on a node opening that hop's evidence, journal rows streamed to the page as deltas with an "as of" stamp per node and feed lost greying the page within 2 s; spec `doctrine/design/operator-experience.md` §2 and §6 | autonomy | planned | | a twelve-row fixture reads at a glance at 1440×900 in the parity screenshot; a delta row reaches the page under 1 s against a fake journal; feed lost greys the page within 2 s in a Playwright test |
| R-75 | the rail is conversation only and hugs the bottom: status, PR, event, activity, plan, confirm, blocker and decision rows leave the thread, the rail opens at the bottom and stays pinned there with one "N new" jump button, long replies fold to a sentence; a Needs-you strip above the tabs shows one question at a time with the recommended option first, keys 1 to 4, a counter, and a Pass to… button with four names; spec §3 and §5 | autonomy | review | | a Playwright test opens the rail scrolled to the bottom, scrolls up, receives a message, and sees the jump button; a thread fixture with every row kind renders only operator, reply and receipt rows; the strip shows exactly one of three open asks, blockers first |
| R-76 | planning is an adversarial interview with a Slack pass: the planner asks up to four questions tagged answerable-by repo, Aaron or a teammate, repo questions go to a scout pass and never reach a person, an open question parks the ticket at Plan and nothing else; a passed question posts to the private `#fd-questions` channel with the teammate @mentioned, in Aaron's voice, only on the click, and the thread reply comes back as the answer, an unclear reply returning to Aaron with the reply attached; spec §3 and §4 | autonomy | planned | | a packet fixture yields questions with tags and the repo-tagged one is answered by the scout without an ask; a passed ask round-trips through a Slack fixture and the ask carries the reply and who gave it; a live post to `#fd-questions` and read-back once the bot exists |
| R-77 | Machine page as a wiring graph: accounts with limit bars, sessions, worktrees and processes as React Flow nodes, locks as edges, observation only; spec §7 | autonomy | planned | | the machine snapshot fixture renders every session and its subtree as nodes with no action button |
| R-78 | scrubber and push: a Flow row replays from its journal under a slider, and a new Needs-you card raises a Windows toast from the desktop app, nothing else ever does; spec §8 | autonomy | planned | | a row at seq N renders the state the fold had at N; one toast per new ask in the desktop tests, none for a status row |
| R-79 | manual stage gates on every Flow row: Pull tickets, Plan, Write goal, Launch agent, Merge and Hand off are buttons, each with an auto toggle that is off by default and fires the stage when the previous one completes; turning every toggle on is the automated pipeline; a backend-only ticket stops at Plan and goes to the backend handoff; spec `doctrine/design/operator-experience.md` §9 | autonomy | planned | | with every toggle off, a pulled ticket sits at Ingest until Plan is clicked and at Plan until Write goal is clicked, proven against a fake Jira feed; with every toggle on, the same ticket reaches Launch with no click; a backend-only packet lands in the backend handoff and never raises an interview ask |
| R-80 | optimistic actions with the standard fallback: every console command carries an `actionId`, the server journals `action.accepted` before any work and answers within 200 ms, the work runs on the tick or a worker and journals `action.done` or `action.failed` with a reason, the store applies the expected state on the click with a pending mark and reconciles on the rows, a failure rolls the element back and shows the reason inline with Retry and Undo; no route blocks on Jira, git, Slack or a process sweep; spec `doctrine/design/operator-experience.md` §10 | clock | planned | | a fake server that never answers still shows the clicked state within one frame and "not confirmed" after 2 s; a fake `action.failed` row rolls the element back with its reason; every command route in a route test answers under 200 ms with a 2 s injected sink |
